services:
  - type: web
    name: fedex-shipping-bot
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: YOUR_CHAT_ID
        sync: false
